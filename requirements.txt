fastapi>=0.115.0
uvicorn[standard]>=0.30.6
sqlalchemy>=2.0.36
# Postgres 驱动：异步运行时用 asyncpg；Alembic 迁移用 psycopg
asyncpg>=0.29.0
psycopg[binary]>=3.2.0
# AKShare 版本锁定 - 接口变动频繁，升级需测试
akshare==1.18.63
openai>=1.50.0
requests>=2.32.0
pandas>=2.2.0
numpy>=1.26.0
scipy>=1.13.0
scikit-learn>=1.5.0
lightgbm>=4.4.0
joblib>=1.4.0
shap>=0.45.0
apscheduler>=3.10.4
pyyaml>=6.0.2
python-dotenv>=1.0.1
pydantic>=2.8.0

# 安全：口令哈希 + 登录限流 + 用户认证
bcrypt>=4.0
slowapi>=0.1.9
prometheus-client>=0.20.0
# fastapi-users: 多用户认证 & JWT 鉴权（替代自研实现）
fastapi-users[sqlalchemy]>=12.0.0
# zxcvbn-python: 密码强度评估（替代自研正则校验）
zxcvbn-python>=4.4.0
prometheus-fastapi-instrumentator>=0.1.0

# 因子评价：alphalens-reloaded（替换自研因子评价，提供 IC/分层收益/换手率等）
alphalens-reloaded>=0.5.0

# 量化回测与AI因子挖掘
# pyqlib: 微软AI量化框架（因子评价/回测/组合分析），CPU密集型，worker进程调用
# 注意：qlib 依赖 mlflow，需 protobuf<4（5.x移除service模块）与 setuptools<81（83移除pkg_resources）
pyqlib>=0.9.6
protobuf<4
setuptools<81
# gplearn: 符号回归/遗传规划自动搜索因子表达式
gplearn>=0.4.2
cvxpy>=1.5
# 数据库迁移
alembic>=1.13

# baostock: A-share daily K-line / valuation / ST-flag data source (baostock_client dep)
baostock>=0.8.9

# 缓存工具：LRU 缓存 IC 评价结果等
cachetools>=5.3.0

# 结构化日志（structlog 替换自研 JSONFormatter）
structlog>=24.4.0

# 重试：tenacity 替代手写重试循环
tenacity>=8.5.0